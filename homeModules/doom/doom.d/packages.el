;;; packages.el --- extra packages -*- lexical-binding: t; -*-
;; Managed by Nix (homeModules/doom/doom.d/). Edit here, then rebuild.
;; Declare extra (unpinned) packages here; Nix builds them declaratively,
;; no `doom sync' needed.

;; e.g. (package! some-package)
;;      (package! another-package :recipe (:host github :repo "user/repo"))
